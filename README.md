# Instalasi Tools
$ pkg update && pkg upgrade

$ pkg install python

$ pip install requests

$ pip install bs4

$ pip install rich

$ git clone https://github.com/SuwanXD/SHARE-V2.git

$ cd SHARE-V2

$ ls

$ git pull

$ python Run.py
